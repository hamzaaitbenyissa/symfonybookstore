{% extends 'base.html.twig' %}

{% block title %}New Genre{% endblock %}

{% block body %}
    <h1>Creation de nouveau genre</h1>

    {{ include('genre/_form.html.twig') }}

    <a class="btn btn-primary" href="{{ path('genre_index') }}">back to list</a>
{% endblock %}
